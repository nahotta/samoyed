# TUTORIAL: ここでコントローラの定義をします
# ※ 定義するコントローラのクラスはclassでないといけません(2.4以降はobjectで定義されたコントローラはサポートされていません）
GET         /          com.example.eventapi.application.controller.EventSearchController.index
GET         /search/:eventId     com.example.eventapi.application.controller.EventSearchController.search(eventId: Long)